stages:
  - test
  - build
  - deploy

test:
  stage: test
  script:
    - echo "Lancement des tests unitaires..."

build:
  stage: build
  script:
    - docker-compose build

deploy:
  stage: deploy
  script:
    - docker-compose up -d
